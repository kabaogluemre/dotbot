{
  "issue_driven": {
    "repository": "",
    "branch_prefix": "feature/issue-",
    "design_doc_root": "docs/designs/",
    "build": {
      "command": "",
      "hint": "Project build command — e.g. 'npm run build', 'dotnet build', 'cargo build'"
    },
    "test": {
      "unit": {
        "command": "",
        "project": "",
        "hint": "Unit test command — e.g. 'npm test -- --selectProjects=unit', 'dotnet test tests/Unit'"
      },
      "integration": {
        "command": "",
        "project": "",
        "hint": "Integration test command — e.g. 'npm run test:integration', 'dotnet test tests/Integration'"
      }
    },
    "labels": {
      "needs_design": "needs-design",
      "ready": "ready",
      "needs_review": "needs-review",
      "needs_qa": "needs-qa",
      "needs_integration_tests": "needs-integration-tests",
      "has_gaps": "has-gaps"
    },
    "commit": {
      "skip_ci_suffix": "[skip ci]"
    }
  }
}
